# PeCa Viewer updates

This repository hosts the signed Sparkle appcast and notarized macOS disk images
for PeCa Viewer. Application source code and update signing private keys are not
stored here.

The latest update feed is [`appcast.xml`](appcast.xml).
